#include <stdio.h>
int main(){
    //int x; 
    //same concept as java, you put the initialisation; final stop point; then increment of decrement counter
    // ++ is used to inc by 1 and -- is to dec by 1 
    for(int x=0;x<10;x++){
        printf("%d\n",x);
    }
    return 1;
}
